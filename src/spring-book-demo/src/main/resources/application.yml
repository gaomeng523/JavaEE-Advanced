spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/book_test?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 118523
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  configuration: # MyBatis 全局配置
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true # 数据库下划线字段自动映射成驼峰属性
  mapper-locations: classpath:mapper/**Mapper.xml
