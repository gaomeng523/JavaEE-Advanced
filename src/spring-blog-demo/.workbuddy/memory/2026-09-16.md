# 2026-09-16

## 全项目代码体检（spring-blog-demo / Spring Boot 3.5.7 + MyBatis-Plus 3.5.5 + jjwt 0.11.5）

结论：能编译、能启动，但登录后所有受保护接口实际全部返回 401，链路被两个断点卡死。

### P0 级故障（功能完全不可用）
1. `common/interceptor/LoginInterceptor.java:18` 判断条件取反：
   `if(!StringUtils.isEmpty(userToken) || JwtUtil.parseJwt(userToken) == null)` →
   带上合法 token 反而 401。应为 `isEmpty(...)`。日志 `userToken:null` 大量刷屏即源于此。
2. 前端没有任何一处发送 `User-Token` 请求头：`static/js/common.js` 是空文件（原计划放 `$.ajaxSetup` 全局请求头）；
   `blog_list.html`、`blog_detail.html` 的 `$.ajax` 均未设 headers。登录页已把 token 写入 localStorage，只是没人用。
3. `blog_login.html:48` `data: JSON.parse({...})` 写错，应为 `JSON.stringify({...})`，否则浏览器直接抛 SyntaxError，登录请求发不出去。

### P1 级（逻辑/契约错误）
- `BlogController` 缺类级 `@Validated`，方法参数上的 `@NotNull` 完全失效；`UserController` 同样有 `@NotNull Integer userId/blogid` 但无类级 `@Validated`，同样无效。
- `BlogServiceImpl.getBlogDetal` 未判空 → `BeanTransfer.trans(null)` 触发 `BeanUtils.copyProperties` 抛 IllegalArgumentException；应抛 `BlogException("博客不存在")`。
- `UserController.getAuthorInfo(Integer blogid)` 参数名 `blogid` 与全局 `blogId` 不一致，前端 `location.search` 传 `?blogId=` 会绑不上。
- `UserServiceImpl` 中 `userInfo.getId() < 0` / `blogInfo.getId() < 0` 判空写法错误（自增主键不可能为负），应为 `== null`。
- `UserLoginRequest` 用 `@NotNull` 而非 `@NotBlank`，空字符串能通过校验。
- `ExceptionAdvice` 的 `MethodArgumentNotValidException` 处理器直接 `getFieldError().getDefaultMessage()`，存在 NPE 隐患。

### P2 级（规范/安全/整洁）
- `JwtUtil` 密钥硬编码在源码；`parseJwt` 失败时把完整 token 打进 error 日志；缺 token 时也记 error，噪音大。
- 密码明文比对（`userInfo.getPassword().equals(...)`），未加密存储。
- `getUserInfo(userId)` 用客户端传的 userId 而非 token 中的 id，存在越权风险。
- `UserService` 接口上挂 `@Service`、接口签名挂 `@NotNull`，无意义。
- `Constant` 里 `import BlogMapper` 未使用，`Constant.DELETE` 未使用；`"User-Token"` 硬编码未抽常量。
- `BlogInfoResponse` 残留 `@TableId` 注解（分层污染）；`UserInfoResponse`/`BlogInfoResponse` 通篇多余。
- 注入风格不统一：`BlogController`/`BlogServiceImpl` 构造注入，`UserController`/`UserServiceImpl`/`WebConfig` 字段注入。
- `WebConfig.addInterceptors` 缺 `@Override`；`/blog/**` 是否该登录待定（列表/详情通常应公开）。
- `ResponseAdvice.supports()` 恒 true，会把 404 等错误响应也套成 `code:200`。
- `application.yml` 明文数据库密码；根目录 `spring-blog.log`/`.gz` 未进 `.gitignore`（仅忽略了 `target/`、`.idea`）。

### 修复进度（18:40 复查日志）
- 用户已自行修好 `LoginInterceptor` 的判断条件（截图确认已是 `isEmpty(...)`），`token 不合法` 的 ERROR 不再出现，短路生效。
- 但 18:40:10 那次请求仍 `userToken:null`，且之后没有任何 Controller 日志和 SQL → 请求在 preHandle 就被 401 掉了。
  准确表述（修正之前的说法）：卡点不是"前端"这个角色，而是**「发出去的请求里没有 User-Token 头」**。用户用 Postman 测试时不经过前端，但同样要手动加这个头；两种情况是同一症状的两个独立来源。
  Postman 侧常见原因：用了 Authorization 标签的 Bearer Token（实际发的是 `Authorization: Bearer xxx`，不是 `User-Token`）、头写在 Params、Headers 行未勾选、名字拼写不一致。
- 另发现前端字段名不一致（待修）：`blog_login.html` 登录 body 用的是 `username`，而后端 `UserLoginRequest` 字段是 `userName`；Spring Boot 默认忽略未知属性，会导致该字段为 null 触发校验失败。

### 历史日志已确认修复的问题
- `Unknown column 'username'`：实体字段曾为 `username`，现已改为 `userName`（映射 `user_name`），18:29 登录 SQL 已正常。
- `Request method 'GET' is not supported`：曾把 `getBlogDetail` 写成 PostMapping，现已是 `@GetMapping`。

---

# 2026-09-18

## 逐项修复（用户要求"逐个解决并给出注释"）

改动清单，全部已加中文注释说明「为什么这么写」：

**P0 链路闭环**
- `js/common.js`：从空文件改为完整实现——`$(document).ajaxSetup` 全局注入 `User-Token`、`ajaxError` 统一处理 401 跳登录、补 `logout()`/`getLoginUserId()`/`escapeHtml()`/`toSummary()`。这是整条链路最关键的一环。
- `blog_login.html`：`JSON.parse` → `JSON.stringify`；`username` → `userName`；input 的 id 同步改名；补失败分支 alert 与回车提交。
- `blog_list.html`：修正 `'<div>'` 未闭合的 bug；加 escapeHtml 防 XSS；空列表兜底；用 `toSummary()` 只展示摘要；文章数改为动态。

**新增能力**
- `Md5Util`：修正 salt 错位（`substring(0,32)` 取的是密文不是盐，应为 `substring(32)`）→ 这是密码校验永远失败的根因；`encryt` 更正为 `encrypt`；补长度兜底与日志。
- `config/MyMetaObjectHandler`（新增）：实现 `MetaObjectHandler`，用 `strictInsertFill`/`strictUpdateFill` 自动填充 createTime/updateTime；实体上加 `@TableField(fill=...)`。
- `BlogServiceImpl`：补齐 `updateBlog`/`deleteBlog`（原先 return null）；加归属校验防越权；`getBlogDetail` 加判空抛 BlogException。

**越权修复**
- `AddBlogRequest` 删掉 `userId` 字段；`LoginInterceptor` 解析 token 后把 userId 塞进 `request.setAttribute`；Controller 用 `@RequestAttribute` 取，并加 `requireLogin()` 兜底。
- `UserController.getUserInfo` 同样改为从 token 取 id，不再接收前端 userId。

**接口签名变更（重要，前端已同步）**
- `BlogService.getBlogDetal` → `getBlogDetail`（拼写修正）
- `BlogService.addBlog(req)` → `addBlog(req, userId)`（前端不再传 userId）
- `BlogService.updateBlog(req)` → `updateBlog(req, userId)`
- `BlogService.deleteBlog(id)` → `deleteBlog(id, userId)`
- `UserController.getAuthorInfo` 参数 `blogid` → `blogId`

**规范/安全**
- `JwtUtil` 从静态工具改为 `@Component` + `@Value("${jwt.secret}")` 注入密钥；日志不再打印完整 token；新增 `getUserId()`。注意：`LoginInterceptor` 必须构造注入 JwtUtil，不能再静态调用（这点曾在改造中漏改一次）。
- `ExceptionAdvice`：BlogException 降为 warn；`getFieldError()` 加判空；兜底分支不再把 `e.getMessage()` 直接返回给前端（防 SQL 泄漏）。
- `ResponseAdvice`：`supports()` 排除文档框架；泛型补全。
- 逻辑删除改用实体字段上的 `@TableLogic(value="0", delval="1")`，而不是 yml 全局配置（全局配置要求所有实体都有该字段）。
- 全部字段注入改为构造注入；`UserService` 去掉无意义的 `@Service`/`@NotNull`。
- `.gitignore` 补 `*.log`/`*.log.gz`/`logs/`。

**未处理（需用户决定）**
- `application.yml` 数据库密码仍是明文（学习项目暂留）。
- MD5 本身不适合生产存密码，已加注释提示换 BCrypt。
- 环境限制：本机 bash 工具基础命令被禁用、无 mvn 可执行文件（`E:\Maven` 只是本地仓库），**未能执行编译验证**，改动仅经静态核对。用户需自行 `mvn compile` 确认。

---

## 前端专项复查 + 前后端联调核对（同日稍后）

用户追问「前端代码有没有问题，以及有没有前后端联合」。逐页读完 5 个 blog_*.html + common.js + 相关 css，并与后端契约逐项比对。

### 新发现并修复的问题

1. **`blog_update.html` 是两个空函数**（最严重）：`submit()` 和 `getBlogInfo()` 都是空壳，页面完全不工作。已完整实现：读地址栏 blogId → 调 `getBlogDetail` 回显 → `editor.setMarkdown()` 写入 → 提交时调 `updateBlog`。
2. **`editormd.markdownToHTML()` 返回值是 undefined**（详情页 bug）：读源码 editormd.js:3895-3972 确认，它不返回 HTML 字符串，而是直接 `div.append(markdownParsed)` 操作 DOM。原来的 `var html = markdownToHTML(...); .html(html)` 会渲染出空白。已改为先渲染进 `#detail-md` 容器，再取 `.html()`。容器移到 script 之前。
3. **`htmlDecode: "style,script,iframe"` 是 XSS 开关**：editormd.js:3955 写着 `sanitize: (settings.htmlDecode) ? false : true`——传了值就会**关闭** HTML 转义，用户能在博客里注入 `<script>`（存储型 XSS）。已去掉该参数。
4. **`editor.config({onload:...})` 用错**：editormd.js:938-955 显示 `config()` 内部会调 `this.recreate()` 销毁重建编辑器。已改为把 `onload` 写进初始化参数。
5. **`editor.getMarkdown()` 早调会抛错**：它依赖 `this.cm`（CodeMirror 异步加载），onload 前是 undefined。已加 `editorReady` 标志位保护（edit 和 update 两个页面）。
6. **`.show()` 会破坏 `display:flex`**：`.content .operating` 是 flex 布局，`.show()` 设为 block 会让按钮竖排。已改用 `.hide` 类 + `addClass/removeClass`，HTML 默认带 hide 类避免闪现。
7. **`ajaxError` 重复弹窗**：详情页并发两个请求都 401 时会弹两次。已加 `redirecting401` 标志位。
8. **`updateById` 误判失败**：MySQL 在「新旧值相同」时返回影响行数 0，原代码 `rows != 1` 就抛异常。已改为不校验行数。
9. 新增 `requireLogin(redirectBack)` 页面守卫 + 登录页支持 `?redirect=` 回跳。
10. `detail.css` 补了 markdown 渲染产物的排版样式（h1-h4/ul/ol/blockquote/pre/code/img/table），原来只有 `p` 有样式。

### 前后端契约核对结果（7 个接口全部对得上）
| 前端调用 | 后端 |
|---|---|
| POST `/user/login` body `{userName,password}` | `UserController.login(@RequestBody UserLoginRequest)` ✅ |
| GET `/blog/getList` | `getList()` ✅ |
| GET `/blog/getBlogDetail?blogId=` | `getBlogDetail(@RequestParam blogId)` ✅ |
| GET `/user/getAuthorInfo?blogId=` | `getAuthorInfo(@RequestParam blogId)` ✅ |
| POST `/blog/addBlog` body `{title,content}` | `AddBlogRequest`（已去掉 userId）✅ |
| POST `/blog/updateBlog` body `{id,title,content}` | `UpdateBlogRequest` ✅ |
| POST `/blog/deleteBlog?blogId=` | `deleteBlog(@RequestParam blogId)` ✅ |
| 请求头 `User-Token` | `Constant.USER_TOKEN_HEADER` ✅ |

### 唯一仍存在的已知限制
- 仍是**静态核对，未编译、未起服务实测**。editormd 的渲染效果、`@TableLogic` 的实际 SQL 需要在浏览器和日志里确认。

### 编译报错修复（用户截图反馈 `@NotNull` 报红）
- 根因：`BlogController` 只写了 `import org.springframework.web.bind.annotation.*;`。
  Java 的 `import 包.*` 是**包级通配**，只覆盖该包下的类，不会连带导入 `jakarta.validation.constraints` 包。
  从 `@RequestParam` 同处使用 `@NotNull` 极易误以为它属于 spring 包，实际属于 Jakarta Bean Validation。
- 修复：补 `import jakarta.validation.constraints.NotNull;`。
- 顺带清理：`UpdateBlogRequest.java` 里 `NotBlank` 只在注释中被提到、并未实际使用，已删除该 import 并把注释改写成明确说明「刻意不加校验」的原因，避免下次又被误认为漏加注解。
- 排查方式：全量 grep `@NotNull|@NotBlank|@Length|@Validated` 的使用点及其 import，确认 `UserController`、`AddBlogRequest`、`UserLoginRequest` 导入均完整。

### 复查「删除/更新博客前端页面是否完成」（用户提问）
结论：**页面不是没写，而是「主流程已通、收尾与边界没做完」**。
两个页面的核心函数都已实现（`blog_update.html` 的 `loadBlogDetail`+`submitBlog`、`blog_detail.html` 的 `deleteBlog`），
缺的是容错与体验部分。本轮补齐：

1. `blog_detail.html` 的 `deleteBlog()`：补 `error` 回调（原来失败静默）、补 `deleting` 防连点闸门
   （连点两次第二个请求会因博客已逻辑删除而报「博客不存在」）、确认弹窗带上文章标题；
   新增全局 `currentBlog` 供读标题。
2. `blog_update.html` 新增 `backToDetail()` + 「返回详情」按钮（原来只能靠浏览器后退，不保存会丢草稿），
   `submitBlog()` 加 `submitting` 闸门并补 400 校验失败的提示分支（`error.responseJSON.errMsg`）。
3. `css/edit.css`：push 区域原本 `#title` 占 78% + `#submit` 占 20%，只有两个控件时刚好填满，
   加了第三个按钮必然挤出去 → 改为 58% / 18% / 18% + 2% margin-left。
4. `blog_list.html` 左侧卡片原本写死「比特汤老师」「GitHub 地址」，与真实登录用户无关。
   新增 `getUserInfo()`，用已有的 `/user/getUserInfo` 接口渲染；`h3`/`a` 补 `id`；
   **未登录时不发这个请求**（该接口在 `WebConfig` 里是需登录的，否则匿名访客逛首页会被 401 踢去登录页）。
5. `blog_detail.html` 左侧卡片仍是写死的「加载中...」，因为 `/user/getAuthorInfo` 只返回 id/userName/githubUrl，
   **没有文章数**，无法填下两行统计 —— 属设计缺口，已告知用户可选项（推荐直接删掉统计行）。

### 待用户实测确认
- 详情页删除 → 列表页不再出现（`@TableLogic` 是否真的生效，看日志 SQL 有没有 `delete_flag = 0`）。
- 更新页回显是否正常（`editor.setMarkdown`）、更新后详情页内容是否变化。
- 首页未登录 vs 登录后左侧卡片文案差异。

---

## 全站前端页面补全（用户要求「结合后端接口完全实现数据真实」）

逐页通读 5 个业务页面 + `common.js` + 4 个 css，把「假数据、静默失败、状态不同步」三类问题全部清掉。

### 1. 假数据清除（核心诉求）
| 位置 | 原状 | 现状 |
|---|---|---|
| `blog_list.html` 卡片用户名 | 写死「比特汤老师」 | `getUserInfo()` 渲染真实 `userName`，未登录显示「未登录」 |
| `blog_list.html` GitHub 链接 | 写死 `<a href="#">` | 有 `githubUrl` 才设 href，否则摘掉 href 显示「暂无 GitHub 地址」 |
| `blog_detail.html` 作者名 | 写死「加载中...」 | `getAuthorInfo()` 渲染 `userName`，失败兜底「匿名用户」 |
| `blog_detail.html` 文章数/分类 | 写死 `2` / `1` | 新增 `countAuthorBlog()`：拉 `/blog/getList` 按 `userId` 前端过滤计数（后端无 count 接口，不为一个数字改后端）；分类仍为 `-`（无数据来源） |

### 2. 静默失败补全
- `blog_edit.html` 的 `submitBlog()`：原来只有 success 回调 → 补 error（400 读 `responseJSON.errMsg`，401 交给 common.js）。
- `blog_login.html` 的 `login()`：原来 error 只处理 400，网络断了/后端没启动时**毫无反应** → 补 else 分支提示。
- `blog_detail.html` 的 `getAuthorInfo()`：接口失败时一直停在「加载中...」→ 补兜底文案。
- `blog_list.html` 空列表：原来只有一句文字 → 改成「还没有博客」+「去写第一篇」链接，`#blogCount` 同步置 0。

### 3. 状态与防连点
- 新增 `submitting` 闸门：`blog_edit.html`（防插两条重复博客）、`blog_update.html`（防两个更新请求）。
- `blog_edit.html` 新增「清空」按钮 `clearEditor()`（markdown 只在内存里，需主动放弃出口）。
- `blog_login.html` 新增 `logining` 闸门 + 已登录访问登录页直接 `location.replace()` 跳走。
- `blog_list.html` `blogInfo.id` 拼 URL 前加 `encodeURIComponent`。

### 4. 导航栏登录态（新增 `renderNavAuth()`）
`common.js` 新增全站共用函数：页面放 `<span id="navAuth">`，未登录显示「登录」，已登录替换成「注销」。
- 列表页/详情页初始写「登录」（允许匿名浏览，不该看到「注销」）。
- 编辑页/更新页初始留空（本就必须登录才能进）。
- 用 DOM API + `$node.on("click")` 绑事件，不依赖 onclick 里的全局函数名。

### 5. `logout()` / 401 处理的隐藏 bug
`ajaxSetup` 只在页面加载时执行一次并写死 `User-Token` 值。原来 `logout()` 只删 localStorage 不重置请求头，
注销后**跳转前那一小段时间**发出的请求仍带旧 token。已在 `logout()` 和 `ajaxError` 里都补 `ajaxSetup({headers:{"User-Token":""}})`。

### 6. CSS
- `edit.css`：`#title` 78% + `#submit` 20% = 98%，加第三个按钮必被挤走 → 改 58/18/18 +2% margin。
- `list.css`：新增 `.right>.empty` 的 flex 居中空状态样式。

### 前后端契约最终核对（全部对齐）
| 前端 | 后端 |
|---|---|
| `POST /user/login` body `{userName,password}` → 读 `data.id`/`data.token` | `UserLoginRequest` / `UserLoginResponse` ✅ |
| `GET /user/getUserInfo` → 读 `data.userName`/`data.githubUrl` | `UserInfoResponse` ✅ |
| `GET /user/getAuthorInfo?blogId=` → 读 `data.id/userName/githubUrl` | ✅ |
| `GET /blog/getList` → 读 `id/title/content/userId/updateTime` | `BlogInfoResponse` ✅ |
| `GET /blog/getBlogDetail?blogId=` | ✅ |
| `POST /blog/addBlog` body `{title,content}` | `AddBlogRequest`（无 userId）✅ |
| `POST /blog/updateBlog` body `{id,title,content}` | `UpdateBlogRequest` ✅ |
| `POST /blog/deleteBlog?blogId=` | ✅ |
| 请求头 `User-Token` | `Constant.USER_TOKEN_HEADER` ✅ |

失败判定：业务失败 = **HTTP 200 + code=-1**（`ResultCodeEnum.FAIL`，不是 500），
所以 success 里必须判 `code == 200 && data == true`；参数校验失败 = HTTP 400 + `errMsg`。

### 仍未做（需用户决定）
- `blog_detail.html` 左侧卡片的「分类」行没有数据来源（后端无分类概念），仍显示 `-`。
- 列表页没有「只看我的博客」筛选（后端 `getList` 不支持按 userId 过滤）。
- 依旧**未编译、未起服务实测**。

---

## 用户贴运行日志后的两个真 bug 修复

用户贴出 22:44 的完整运行日志，暴露两个问题，其中一个是**我上一轮改出来的**。

### 好消息：之前修的都验证通过
`SELECT ... FROM blog_info WHERE delete_flag=0` —— `@TableLogic` 确实生效，逻辑删除条件自动拼接。
`getBlogDetail`/`getAuthorInfo` 均正常返回，数据是真实的。

### Bug 1（我上轮引入的）：`ajaxSetup` 静态请求头是万恶之源
**上一轮的坏改动**：为了让「注销后到跳转前那一小段窗口」不带旧 token，
我在 `logout()` 和 401 处理里加了 `ajaxSetup({headers:{"User-Token":""}})`。
结果 401 时把 header 打成空串后**永久无法恢复**，后续所有请求（含删除）全部丢失 token →
日志出现两次 `请求被拦截：未携带 token，uri=/blog/deleteBlog`，删除彻底失效。
**修一个理论上的小瑕疵，造出一个真 bug。教训：不要为了边界场景引入持久副作用。**

**根治方案**：改用 `$(document).ajaxSend(fn)` 每次请求**现读** localStorage 并 `setRequestHeader`。
- `ajaxSetup({headers: {...}})` 只在页面加载时求值一次，值是**静态字符串**——这是所有此类问题的根源。
- 有 token 才设置头，没 token 就不发这个 header（而不是发一个空串）。
- 之后 `logout()` / 401 处理只需删 localStorage，header 自动跟着变，不需要任何手工同步。

### Bug 2：401 死循环 + 首页被过度反应
日志里反复出现 `请求被拦截：未携带 token，uri=/user/getUserInfo`。
链路：localStorage 残留**失效**旧 token → 首页 `isLogin()` 返回 true（只看有没有值）→ 发请求 → 401
→ 全局 `ajaxError` 弹窗 + 删 token + **强制跳登录页** → 用户在自己想看的首页上被踢走。

两处修复：
1. `blog_list.html` 的 `getUserInfo()` **就地处理 401**：清失效凭证、卡片恢复「未登录」、`renderNavAuth()` 重渲染，页面照常用。
   理由：这个请求只影响左侧卡片，失败不该影响整页；首页本就允许匿名浏览，跳登录页是过度反应。
2. `blog_login.html` 去掉「已登录就无条件跳走」：**只在有 `redirect` 参数时才跳**。
   否则「跳首页 → 首页 401 → 跳回登录页 → 又判定已登录 → 再跳走」会成死循环，用户永远登不上。
   另加 `leaving` 标志位：`location.replace()` 是异步的，挂起期间用户点「提交」会被跳转冲掉。

### 顺带：详情页重复查询 → 改为后端真统计
日志显示打开详情页时 `SELECT ... WHERE (id = ?)` **并行执行了两遍**，且多一次全表 `getList`。
根因是我上一轮让 `countAuthorBlog()` 去拉 `/blog/getList` 再前端过滤计数——为了一个数字拉全表 + 传全部正文。

改为后端真统计（用户要求「数据真实」，本该如此）：
- `BlogService.countByUserId(Integer)` → `BlogServiceImpl` 用 `blogMapper.selectCount(...eq(userId))`，只回一个数字。
- `BlogController` 新增 `GET /blog/countByUser?userId=`。
- **该接口不加进拦截器**：统计的是任意作者的公开文章数，与「当前登录人」无关，匿名看详情页也要能调。
  在 `WebConfig` 注释里补了判断标准：*这个接口会按「当前登录人」返回数据、或修改数据吗？会就加。*

---

## 「为什么没有删除博客」——按钮显示条件比鉴权条件宽松

用户截图：详情页显示 `lisi` / 文章数 `1`（真实数据，`countByUser` 接口已生效），
但**页面没有编辑/删除按钮**，且导航栏显示「登录」而非「注销」→ 当前是未登录态。

### 真 bug：`blog_detail.html` 的按钮显示条件缺了登录判断
```js
// 原来（有漏洞）
if (String(blogInfo.userId) === String(getLoginUserId())) { 显示按钮 }
```
只比 userId，**没检查是否已登录**。危害：localStorage 里残留 `loginUserId` 但 token 已失效时，
`String(1) === String(1)` 成立 → **按钮显示出来，点下去必然 401**（拿不到 User-Token）。
**按钮的显示条件绝不能比服务的鉴权条件更宽松** —— 这正是日志里 `deleteBlog` 401 的产生方式。

修复：`isLogin() && String(userId) === String(getLoginUserId())`，两个条件缺一不可。

### 附带：未登录时明确告知原因
未登录访问他人博客本来就不该有编辑按钮，但页面上什么都不显示会被**误认为「删除功能坏了」**。
新增 `.content .login-tip`（默认 `hide`）：未登录时显示「登录 后可管理自己的博客」，并在 `detail.css` 补样式。

### 顺带确认的数据链路
截图里文章数显示 `1`，说明新增的 `GET /blog/countByUser` 已正常工作（`lisi` 只有 1 篇）。
导航栏「登录」文字证明 `renderNavAuth()` 也在按登录态正确渲染。


