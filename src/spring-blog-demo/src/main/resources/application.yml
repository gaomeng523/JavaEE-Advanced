spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/java_blog_spring?characterEncoding=utf8&useSSL=false
    username: root
    password: 118523
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 逻辑删除不在这里做全局配置，而是在实体字段上用 @TableLogic 显式标注。
# 全局配置的 logic-delete-field 要求每个实体都有该字段，且排查问题时不够直观。

# JWT 签名密钥（Base64 编码）。生产环境应通过环境变量注入，不要提交到代码仓库
jwt:
  secret: "+JEq/o2bYDEtaECkxKCeAt0i3yA0IPUlV2rxFVGS+v4="

logging:
  file:
    name: spring-blog.log
