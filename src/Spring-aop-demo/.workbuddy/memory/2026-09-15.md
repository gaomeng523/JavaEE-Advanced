# 2026-09-15

## 把《12. Spring AOP》课件的全部示例代码落到 Spring-aop-demo 项目
- 来源：`C:\Users\a\Desktop\课程学习课件\JavaEE进阶课件\12. Spring AOP.pdf`（37 页）
- 方式：用 python + pypdf 抽取 PDF 文本（PDF 超出 Read 工具 256KB 上限），再逐节整理成代码。

### 新增/改动文件
- `common/Result.java`、`model/BookInfo.java`（课件写"...代码省略"，补全为可运行实现）
- `controller/BookController.java`（2.2 耗时统计目标，内存 Map 模拟数据库）
- `controller/TestController.java`（改写：映射改为 `/test`，t1 加 @MyAspect，t2 抛 10/0）
- `controller/UserController.java`（3.5.2，u1 加注解、u2 不加）
- `controller/PriorityController.java`（3.4，替代课件里"先注释掉 AspectDemo 的 @Component"的做法）
- `aspect/TimeAspect.java`（2.2 @Around 耗时统计）
- `aspect/AspectDemo.java`（3.2 五种通知 + 3.3 @Pointcut，pt() 设为 public 供跨类引用）
- `aspect/AspectDemo2.java`（3.3 跨切面引用 `AspectDemo.pt()`）
- `aspect/MyAspectDemo.java`（3.5.2 @annotation 切点）
- `aspect/priority/PriorityAspectDemo1/2/3.java`（3.4 @Order(1)(2)(3)）
- `annotation/MyAspect.java`（3.5.2.1 自定义注解）
- `proxy/`：HouseSubject、RealHouseSubject、HouseProxy、StaticProxyMain、JDKInvocationHandler、
  JDKDynamicProxyMain、CGLIBInterceptor、CGLIBDynamicProxyMain（4.1 代理模式）
- `proxy/spring/`：SpringRealHouseSubject、SpringHouseProxy、SpringProxyAspect、SpringProxyMain（4.2）
- `application.yaml`：补注释说明 `spring.aop.proxy-target-type` 开关

### 关键决策
- CGLIB 用 Spring repackage 的 `org.springframework.cglib.proxy.*`（spring-core 自带），
  不需要课件里说的额外 `cglib:cglib:3.3.0` 依赖。
- 4.2 的 demo 用非 Web 方式启动上下文，避免和主启动类抢 8080 端口。
- 3.4 的三个切面改名为 PriorityAspectDemoN，解决 Bean 名冲突。

## 清理注释中的"出处"说明（用户要求）
- 用户要求：保留关键注释，但去掉注释里"来自哪个课件"的出处标注。
- 已把所有 `课件 X.Y`/`课件原文`/`对应课件里的 XXX` 等表述去掉，只保留知识点本身，
  并顺手补回 `TestController.t1()` 上丢失的 `@MyAspect`（否则 MyAspectDemo 的注释与实际不符）。
- 全量 `grep 课件` 已无命中；`mvn -B clean test` → BUILD SUCCESS。
- 用户偏好记录：注释里**不要写课件章节号/出处**，直接讲知识点即可。
